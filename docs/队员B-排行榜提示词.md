# 队员B 提示词：排行榜前端页面

把这个提示词直接复制，发给 Claude Code 或其他 AI Agent 即可。

---

## 粘贴给 Agent 的完整提示词：

```
帮我创建一个排行榜前端页面，放在 frontend/leaderboard/ 目录下。

## 要求：

### 文件结构
在 frontend/leaderboard/ 下创建以下文件：
- index.html（页面结构）
- style.css（样式）
- leaderboard.js（数据获取 + 排序展示逻辑）

### 功能需求：
1. 调用后端 API 获取排行榜数据：GET http://localhost:5002/api/leaderboard
   后端返回格式：{ "scores": [{"player":"张三","time":35,"level":1}, {"player":"李四","time":42,"level":1}, ...] }
2. 将获取到的成绩按通关时间从小到大排序（升序，时间短的排前面）
3. 用表格展示排行榜，包含：排名、玩家名、通关时间（秒）、关卡
4. 前三名用金🥇银🥈铜🥉标记
5. 每行用不同底色交替显示（斑马条纹）
6. 有一个"刷新"按钮重新获取数据
7. 顶部导航栏，可以跳转到迷宫游戏页面（../maze-game/index.html）

### 样式要求：
- 页面居中，深色背景
- 表格清晰美观，宽度 600px
- 第一名金色高亮，第二名银色高亮，第三名铜色高亮
- 斑马条纹更易阅读

### 用到的数据结构（在注释里标注）：
- 数组：存储排行榜数据列表
- 排序算法：对成绩按时间排序（JavaScript 的 sort 方法，基于比较的排序）
- 在 leaderboard.js 里用注释标注：
  "/* 数据结构：数组，存储排行榜记录，使用比较排序按通关时间升序排列 */"
  "/* 时间复杂度：O(n log n)，使用 JavaScript 内置排序 */"

请一次性生成所有完整代码，不要省略。
```
