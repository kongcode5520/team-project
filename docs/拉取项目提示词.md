# 拉取项目提示词

把这个提示词直接复制，发给 Claude Code 即可。

---

```
帮我把团队项目拉取到本地并运行起来。

## 步骤：

1. 克隆仓库：
   git clone https://github.com/kongcode5520/team-project.git
   cd team-project

2. 安装依赖（两个后端都需要）：
   pip install flask flask-cors

3. 启动项目（右键 start.py → Run），会自动启动：
   - 迷宫 API (port 5001)
   - 游戏 API (port 5002)
   - 前端服务 (port 5500)
   浏览器会自动打开迷宫游戏页面

4. 如果浏览器没自动打开，手动访问：
   http://localhost:5500/maze-game/index.html

## 注意：
- 确保电脑已安装 Python 3
- 如果 pip 报错，试试 python -m pip install flask flask-cors
- 如果端口被占用，先关掉其他 Python 进程
```
